# add_lunch_combo is obsolete on lineage-21 (Android 14's three-part <product>-<release>-<variant>
# lunch combos) -- COMMON_LUNCH_CHOICES in AndroidProducts.mk is the current mechanism and already
# covers this. File kept, deliberately empty, so the source tree's usual `source
# device/oculus/monterey/vendorsetup.sh`-style convention still finds something here.
