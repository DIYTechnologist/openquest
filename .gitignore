# ─── Device dumps & backups (huge, per-unit, sensitive: serials/keys/calibration) ───
/backups/
/dumps/
/work/
/*.zip
/1PASH9ACHD0215-*

# ─── Downloaded toolchains & sources (regenerable — see README) ───
/tools/android-ndk-r27c/
/tools/android-ndk-r27c-linux.zip
/tools/aosp-headers/
/tools/hidl-build/hidl/
/tools/hidl-build/libbase/
/tools/hidl-build/gen/
/tools/hidl-build/obj/
/tools/hidl-build/hidl-gen
/tools/hidl-build/*.tgz
/tools/hidl-build/*.b64

# ─── Pulled device binaries (redistribution-questionable; RE findings live in notes/) ───
/recon/hal-A-2026-08-31/devlibs/
recon/**/*.so
recon/**/*debugdata*
recon/hal-A-2026-08-31/vendor.oculus.hardware.sensors@1.0-service
recon/config
recon/config.gz

# ─── Large binary artifacts (keep the small JSON/YAML calibration next to them) ───
exports/**/*.mura
exports/**/*.tar
exports/**/*.bin
exports/**/sns.reg

# ─── Large captured logs (small text findings are kept) ───
recon/**/dmesg.txt
recon/**/logcat-all.txt

# ─── Compiled probe/client binaries (sources are kept) ───
tools/hal_probe/hal_probe
tools/hal_stream/hal_stream
tools/hal_stream/hal_stream2
tools/hal_stream/hal_stream3
tools/hal_stream/hal_stream4
tools/hal_stream/hal_stream5
tools/hal_stream/clienttest
tools/imu_intercept/*.so

# ─── Editor / local tooling caches ───
.tau/

# ─── Editor / local ───
.claude/settings.local.json
*.o
*.swp

# camera frame raws (keep PNGs)
exports/**/*.gray
exports/**/*.pgm
tools/cam_tap/*.so
tools/dmabuf_read/dmabuf_read
tools/dmabuf_read/*.gray
tools/mempeek/mempeek

# VIO dataset frames (bulky; CSVs/calib/README kept)
exports/**/cam0/data/*.png
tools/cam_tap/*.so

exports/**/cam1/data/*.png
exports/vio-stereo*/**/*.png

exports/vio-precise/**/*.png
exports/vio-precise/**/*.pgm

# direct-capture raw frames + built EuRoC dataset (bulky; CSVs/notes kept)
exports/vio-direct-*/raw/
exports/vio-direct-*/euroc/
exports/vio-direct-*/sbout/
exports/**/syncboss.raw

# VIO experiment scratch (regenerable from raw/ via tools/vio/build_euroc_direct.py)
exports/vio-direct-*/trunc*/
exports/vio-direct-*/euroc_t/
exports/vio-direct-*/rect/
exports/vio-direct-*/ovconfig_dbg/
exports/vio-direct-*/ov_*.txt

# Superseded by build/containers/openvins-android-deps/ and build/containers/kernel-toolchain/ --
# these host-side caches are no longer how components/tracking or components/kernel are built, but
# the entries are kept in case an old checkout still has them.
work/android-deps/
work/kbuild/aarch64-linux-android-4.9/
work/kbuild/arm-linux-androideabi-4.9/
work/kbuild/out/
__pycache__/

# ─── Component build outputs (research-notes/53-era reorg: reproducible via `make`, not
#     committed -- see each components/*/Makefile) ───
/components/camera/cam_kernel
/components/camera/.kinc/
/components/controllers/sb_leech
/components/tracking/vio_live
/components/tracking/pose_inject
/components/tracking/replay_feed
/components/tracking/ov_bench
/components/tracking/build/
/components/kernel/build/

# tools/cam_kernel/build.sh regenerates this the same way; never committed.
tools/cam_kernel/.kinc/
